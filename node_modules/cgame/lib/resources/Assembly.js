var Resource  = require('./Resource.js');
var __extends = require('../util/extends.js');

module.exports = (function(_super) {
  __extends(Assembly, _super);

  // A list of components and their config values that an entity system can
  // create. Used to streamline entity creation
  function Assembly()
  {
    _super.apply(this, arguments);
    this.components = [];
    this.config     = {};
  }

  return Assembly;

})(Resource);
