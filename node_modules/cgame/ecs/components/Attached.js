module.exports = (function() {

  // Give an entity the ability to be "attached" to a parent to follow
  // position or rotation. Any components that may have different behavior
  // when attached (for example, Position), should implement "attached-aware"
  // helper functions there.
  function Attached()
  {
    this.parentEntity  = null;
  }

  return Attached;

})();
