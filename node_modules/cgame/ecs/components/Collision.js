module.exports = (function() {

  // Event for when a collision occurs
  Collision.coincident = 'coincident';

  // Gives an entity the ability to register collisions with anythign that
  // has a Spatial component
  function Collision()
  {
    this.active     = false;
    this.ignoreFlag = null;
  }

  return Collision;

})();
