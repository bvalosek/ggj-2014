module.exports = (function() {

  function System(manager)
  {
    // Injected by system manager
    this.manager = manager;
  }

  // Proxy listen
  System.prototype.listenTo = function(event, Components, callback)
  {
    return this.manager.listenTo(event, this, Components, callback);
  };

  return System;

})();
